Experimental S721W DZF4 profile

Local build:
make TARGET=r12s-S721WVLSCDZF4 clean all release

GitHub Actions: use the workflow_dispatch target selector `s721w` to build only the W profile, or `all` to build both profiles.
